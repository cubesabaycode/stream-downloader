services:
  - type: web
    name: direct-stream-downloader
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python stream_downloader.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0