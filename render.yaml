services:
  - type: web
    name: stream-downloader
    env: python
    plan: free
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn --bind 0.0.0.0:$PORT stream_downloader:app"
    # Removed envVars for PYTHON_VERSION as it is usually inferred or set globally
